<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Funding Congress</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
      </ul>
      <ul class="account nav navbar-nav navbar-right">
        <% if current_user %>
          <li><%= link_to "My Legislators", legislators_path %></li>
          <li><%= link_to "Logout", logout_path, id: "logout" %></li>
          <li><a class="navbar-image" href="/users/<%= current_user.id %>"><img src="<%= current_user.image_url %>"  alt="facebook avatar" /></a></li>
        <% else %>
          <li><a id="login" href="/login">Login <i class="fa fa-facebook-square"></i></a></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
